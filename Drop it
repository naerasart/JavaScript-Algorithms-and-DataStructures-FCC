function dropElements(arr, func) {var x=[];
  for (var i=0; i<arr.length; i++)
   if (func(arr[i])==true) {return x=arr.slice(i);}
   else{ x;}
return x;
   
}
 
dropElements([1, 2, 3], function(n) {return n < 3; });
