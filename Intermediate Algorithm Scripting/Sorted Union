function uniteUnique(...arr) {
   var a = arr.flat() ; //to flatten an array;
   let u  = [...new Set(a)]; // to delete duplicates;
   console.log (u);
 return  u;
}
  


uniteUnique([1, 3, 2], [5, 2, 1, 4], [2, 1]);
