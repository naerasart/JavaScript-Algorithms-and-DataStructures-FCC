function diffArray(arr1, arr2) {
     
   var y=[];
    y=arr1.concat(arr2);
    var x=[];
    x=arr2.filter(item => arr1.indexOf(item) === -1).concat(
    arr1.filter(item => arr2.indexOf(item) === -1));
    console.log(x);
    return x;
}

diffArray([1, 2, 3, 5], [1, 2, 3, 4, 5]);
 
